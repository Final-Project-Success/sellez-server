{
  "name": "users",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --runInBand --forceExit --setTimeout=500 --verbose --coverage",
    "db:test": "sequelize --env=test db:create && sequelize --env=test db:migrate:undo:all && sequelize --env=test db:migrate",
    "start": "node app.js",
    "dev": "nodemon app.js",
    "db:create": "sequelize db:drop && sequelize db:create",
    "db:migrate": "sequelize db:migrate:undo:all && sequelize db:migrate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "otp-generator": "^4.0.0",
    "pg": "^8.8.0",
    "sequelize": "^6.28.0"
  },
  "devDependencies": {
    "jest": "^29.3.1",
    "nodemon": "^2.0.20",
    "supertest": "^6.3.3"
  }
}
